<h1> Ultimate Train System </h1>

<h3> Cities in our Network </h3>
<hr>
<% if @cities.any? %>
  <ul>
    <% @cities.each do |city| %>
      <li><a href="/cities/<%= city.id %>"><%= city.name %></a></li>
    <% end %>
  </ul>
<% else %>
  <p>There are currently no cities to display.</p>
<% end %>

<a href="/cities/new">Add a new City</a>